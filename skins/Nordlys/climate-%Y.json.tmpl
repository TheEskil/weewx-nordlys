## Per-year climate slice: climatological days, calendar heatmap, and
## year stats for one year. The Climate page's year picker fetches this
## and swaps the year-scoped tiles in place. Immutable for past years, so
## weewx's SummaryByYear staleness generates each once.
$nordlys_climate_json
