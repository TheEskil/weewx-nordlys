{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/eskil/weewx-nordlys/docs/config.schema.json",
  "title": "Nordlys resolved skin configuration",
  "description": "The `config` object of the Nordlys data payload - the JSON form of skin.conf [Nordlys], as serialized by the search-list extension. See docs/config-reference.md.",
  "type": "object",
  "required": ["pages"],
  "properties": {
    "theme": {
      "type": "object",
      "properties": {
        "mode": { "enum": ["auto", "dark", "light"] },
        "switcher": {
          "type": "boolean",
          "description": "Show the header theme switch (default true)."
        },
        "dark": { "$ref": "#/$defs/tokenOverrides" },
        "light": { "$ref": "#/$defs/tokenOverrides" }
      },
      "additionalProperties": false
    },
    "live": {
      "type": "object",
      "required": ["broker"],
      "properties": {
        "broker": { "type": "string", "pattern": "^wss?://" },
        "topic": { "type": "string" }
      },
      "additionalProperties": true
    },
    "pages": {
      "type": "array",
      "items": { "$ref": "#/$defs/page" }
    }
  },
  "additionalProperties": true,
  "$defs": {
    "tokenOverrides": {
      "type": "object",
      "description": "Any --nl-* design token, without the prefix",
      "additionalProperties": { "type": "string" }
    },
    "page": {
      "type": "object",
      "required": ["id", "title", "layout"],
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "picker": {
          "enum": ["week", "month", "year"],
          "description": "Add a period dropdown to jump to past weeks/months/years."
        },
        "layout": {
          "type": "array",
          "items": { "$ref": "#/$defs/row" }
        }
      },
      "additionalProperties": false
    },
    "row": {
      "type": "object",
      "required": ["tiles"],
      "properties": {
        "title": { "type": "string" },
        "columns": { "type": "integer", "minimum": 1, "maximum": 6 },
        "tiles": {
          "type": "array",
          "items": { "$ref": "#/$defs/tile" }
        }
      },
      "additionalProperties": false
    },
    "tile": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "enum": [
            "gauge",
            "stat",
            "chart",
            "table",
            "text",
            "climatology",
            "celestial",
            "forecast",
            "reports"
          ]
        },
        "obs": {
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ]
        },
        "title": { "type": "string" },
        "options": { "$ref": "#/$defs/tileOptions" }
      },
      "additionalProperties": false
    },
    "tileOptions": {
      "type": "object",
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "style": { "enum": ["compass", "cards"] },
        "color": { "type": "string" },
        "colors": {
          "type": "array",
          "items": { "type": "string" }
        },
        "cold_below": { "type": "number" },
        "hot_above": { "type": "number" },
        "chart": {
          "enum": ["line", "area", "bar", "scatter", "windrose", "calendar"]
        },
        "span": {
          "enum": ["24h", "day", "yesterday", "week", "month", "year", "alltime", "archive"]
        },
        "table": { "enum": ["stats", "records"] },
        "aggregate": { "enum": ["avg", "min", "max", "sum"] },
        "stats": {
          "type": "array",
          "items": { "type": "string" },
          "description": "reports tile: obs shown as at-a-glance period stats in the browser."
        },
        "section": {
          "enum": ["sun", "sunpath", "moon", "seasons", "planets"],
          "description": "celestial tile: which almanac section to render (bare tile = compact combo card)."
        },
        "bands": {
          "type": "array",
          "items": { "type": "number" },
          "minItems": 1
        },
        "calm_below": { "type": "number" },
        "always_show": {
          "type": "boolean",
          "description": "Keep the tile visible even when its observation has no data (absent sensor)."
        }
      },
      "additionalProperties": true
    }
  }
}
